## 1. 标注任务投影

- [x] 1.1 扩展标注查询读取结果，使一次调用返回全部 Query 及当前候选的总数、已标注数和命中数，并用 `tests/test_annotation_progress.py` 验证历史候选不计入聚合且零候选可识别。
- [x] 1.2 实现批量与单图任务 payload 的安全规范化和任务到 Query 的只读关联，覆盖重复路径、缺失/非法 payload 与非查询任务，并用新增单元测试验证不发生数据库写入。
- [x] 1.3 实现任务标注汇总、四态 Query 状态、默认 Query 选择和“未归档查询”投影，并用单元测试验证同一 Query 可被多个任务引用、未归档数据不丢失及零候选不计完成。

## 2. 任务列表与页面状态

- [x] 2.1 将标注页入口改为按当前数据库加载标注任务投影，并实现带任务名称、执行状态、Query 数、完成数和进度的任务数据表；用 UI/辅助函数测试验证索引任务被排除且零 Query 任务不可进入。
- [x] 2.2 实现从任务表进入工作台、从工作台返回任务表，以及以 `task_id`/`query_id` 保存稳定 session 状态；用测试验证首次进入优先未完成 Query、全完成时选择首项。
- [x] 2.3 兼容搜索页传入的 `annotation_query_id`，自动定位包含目标 Query 的最新可用任务或“未归档查询”，并用 `tests/test_workspace_state.py` 及新增导航测试验证直接入口。

## 3. Query 数据导航与结果标注

- [x] 3.1 在任务工作台增加可单行选择的 Query 数据导航，展示文件、路径、四态状态、已标注数/总数和命中数；用纯函数测试验证选择行稳定映射到 `query_id`。
- [x] 3.2 以选中 `query_id` 驱动现有查询锚点、历史候选开关和候选网格，确保逐结果命中/未命中/跳过、备注和改标能力不变，并运行现有标注与工作台测试验证回归。
- [x] 3.3 删除“上一题”“下一题”“跳至未标注”及 Enter 下一题绑定，只保留候选焦点导航和快捷键帮助；用 UI 测试或渲染断言验证顺序切换控件不再出现。
- [x] 3.4 确保每次结果标注 rerun 后仍选中同一 Query，并同步刷新 Query 四态和任务完成进度；用覆盖最后一个候选完成、已完成 Query 改标的测试验证一致性。

## 4. 验证与交付

- [x] 4.1 运行标注、任务、工作台和状态相关的定向 pytest 测试，并修复所有失败。
- [x] 4.2 运行完整 `uv run pytest` 与 `git diff --check`，确认无回归、无格式错误，并确认未修改 schema 或新增迁移脚本。

## 5. 固定按钮导航与横向结果布局

- [x] 5.1 将 Query 单行选择数据表替换为逐 Query 导航按钮，以 `query_id` 保持当前选择并直接切换；通过 UI 测试验证按钮可选择任意 Query、当前项可识别且不再渲染 Query 数据表。
- [x] 5.2 为 Query 导航增加固定可见容器、四种状态颜色、状态文字或图标以及当前项边框；通过样式契约测试验证 sticky、横向溢出、四态样式和非颜色提示均存在。
- [x] 5.3 将候选结果改为宽卡片、单行不换行的横向滚动轨道，保持逐结果标注、历史候选、备注和键盘焦点能力；通过 UI 与回归测试验证多结果不换行且所有标注动作可用。
- [x] 5.4 运行标注导航相关定向测试、完整 `uv run pytest`、`git diff --check` 和 OpenSpec 严格校验，记录任何与本次变更无关的既有失败，并确认未修改 schema 或新增迁移脚本。
