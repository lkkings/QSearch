顺序遵循 design.md「Migration Plan」：备份 → 队列 schema → 标注库迁移 → 池与缓存 → 车道 → 页面 → 图标 → 验证。第 4、5 组是第 6、7 组的前提（页面化后的搜索页要调用交互车道）。

## 1. 备份与基线

- [x] 1.1 备份 `data/databases/*/annotations/annotations.db` 与 `data/queue/tasks.db` 到带时间戳的目录，验证备份文件可被 `sqlite3 <file> "PRAGMA integrity_check"` 通过
- [x] 1.2 建立检索结果基线：选取覆盖完全匹配与内容匹配的 10 张查询图，记录其候选集合、排名与各项评分到基线文件，验证基线文件含全部 10 条且每条有非空候选（供 4.5 比对 `SearchEngine` 拆分正确性）
- [x] 1.3 用 `scripts/seed_test_data.py` 或等价方式准备一个含重复 `(database_name, query_image_path)` 行的测试标注库，验证 `SELECT database_name, query_image_path, COUNT(*) ... HAVING COUNT(*) > 1` 返回非空（供第 3 组迁移测试）

## 2. 队列 schema：车道与并行度列

- [x] 2.1 在 `src/qsearch/tasks/schema.sql` 为 `task_queue` 加 `lane` 列（NOT NULL，默认 `background`，CHECK 限定 `background`/`interactive`），验证在既有 `tasks.db` 上执行迁移后所有历史行的 `lane` 均为 `background`
- [x] 2.2 将 `task_queue.kind` 的 CHECK 扩展为含 `interactive_search`，验证插入一行 `kind='interactive_search'` 成功且插入非法 kind 仍被拒绝
- [x] 2.3 删除 `task_queue.num_workers` 列，验证迁移后表结构无该列且既有 pending 行仍可被 `claim_next` 取出
- [x] 2.4 加索引 `(lane, status, created_at)` 支撑按车道取任务，验证 `EXPLAIN QUERY PLAN` 对车道过滤的取任务语句命中该索引
- [x] 2.5 编写 `tasks.db` 迁移脚本（幂等），验证在已迁移库上重复执行不改变表结构且不报错

## 3. 标注库：唯一身份与迁移

- [x] 3.1 编写迁移的报告模式：按 `(database_name, query_image_path)` 分组统计将合并的分组数与 label 冲突条目数，验证在 1.3 的测试库上输出的分组数与手工计数一致且不修改任何数据
- [x] 3.2 实现合并逻辑：保留 `created_at` 最早的 `query_id` 为主行，其余行候选按 `candidate_image_path` 归并，label 冲突取 `labeled_at` 最新的非空值，验证合并后测试库中每个 `(database_name, query_image_path)` 只剩一行且原有非空 label 数量未减少
- [x] 3.3 验证迁移幂等：在已迁移库上再次执行，验证 `queries` 与 `candidates` 的行数与内容校验和均不变
- [x] 3.4 在 `src/qsearch/webui/schema.sql` 为 `queries` 加 `UNIQUE(database_name, query_image_path)`、为 `candidates` 加 `UNIQUE(query_id, candidate_image_path)`，验证在已合并的库上建约束成功，且重复插入被拒绝
- [x] 3.5 为 `candidates` 加 `in_current_result` 列（NOT NULL，默认 1），验证历史行迁移后该列全为 1
- [x] 3.6 重建 `update_query_status` 触发器使其排除 `in_current_result = 0` 的行，验证「当前结果集内候选全部标注 + 存在一个未标注的历史候选」时 query 状态为 `completed`
- [x] 3.7 重建 `candidate_stats` 视图使统计只计入 `in_current_result = 1` 的候选，验证视图中某含历史候选的 query 其 `total_candidates` 不含历史候选

## 4. 进程池：池键降维与两层缓存

- [x] 4.1 在 `src/qsearch/tasks/worker_pool.py` 把池键从 `(kind, spec_key, num_workers)` 改为 `(kind, model_config_hash)`，验证两个 `database_path` 不同但模型配置相同的 spec 产出同一个池键
- [x] 4.2 实现 worker 内第一层缓存：`FeatureExtractor` 按模型配置哈希缓存，容量 1，验证同一 worker 处理两个不同数据集的任务时提取器只构造一次（以构造计数或日志断言）
- [x] 4.3 实现 worker 内第二层缓存：索引与 matcher 按 `database_path` LRU 缓存，容量 4，验证依次访问 5 个数据集后最早访问的那个被淘汰且第 5 个查询结果正确
- [x] 4.4 拆分 `src/qsearch/webui/search_engine.py`：分离「共享提取器」与「按库索引」，`search_single` 接受注入的提取器，验证不注入提取器时行为与改动前一致（既有调用方不破）
- [x] 4.5 用 1.2 的基线比对拆分后的检索结果，验证 10 条查询的候选集合、排名与各项评分与基线完全一致（任何不一致视为拆分错误，不接受近似）
- [x] 4.6 在 `src/qsearch/tasks/settings.py` 新增 `clamp_interactive_workers`（默认 2，上限 4），验证请求 0 收敛到 1、请求 99 收敛到 4、非整数回退默认值
- [x] 4.7 实现启动时内存预算估算与降级：估算 `(N + M) × WORKER_MEMORY_GB`，超出可用内存时降低交互并行度而非后台并行度，验证构造的低内存场景下 M 被降低、N 保持请求值、且输出含降级原因与降级后的值

## 5. 调度器：双车道与交互 runner

- [x] 5.1 让 `TaskQueue.claim_next` 接受车道参数并只取该车道的 pending 行，验证交互车道的 claim 不会取到后台车道的任务，反之亦然
- [x] 5.2 从 `TaskQueue.enqueue` 移除 `num_workers` 参数、新增 `lane` 参数，验证入队索引构建与批量检索归入后台车道、入队单图查询归入交互车道
- [x] 5.3 在 `src/qsearch/tasks/scheduler.py` 改为双取任务循环，每车道一个 `PoolRegistry` 且互不共享，验证后台车道执行长任务期间交互车道仍能取走并完成一个交互任务
- [x] 5.4 让运行中任务集合与心跳按车道分组，验证两车道各有任务在跑时心跳同时刷新两者、且一个车道的任务失败不影响另一车道任务的心跳
- [x] 5.5 后台车道并发固定为 1、交互车道并发取交互并行度，验证连续提交两个后台任务时第二个保持排队，而交互并行度为 2 时两个交互任务并发执行
- [x] 5.6 新增 `src/qsearch/tasks/runner_interactive.py`：执行单图检索、结果写入 `annotations.db`、不记录断点位置，验证任务完成后该 Query 与候选出现在标注库中
- [x] 5.7 让交互任务失败时不向 `annotations.db` 写入，验证失败后标注库中不存在该 Query 的记录且任务携带可读错误信息
- [x] 5.8 实现交互车道终态行清理：删除 `lane='interactive'` 且处于终态且超过保留窗口的行，验证过期交互行被删除、后台终态行保留、排队中与运行中的交互行不被删除（三个条件各写一个反例）
- [x] 5.9 让取消或异常结束的任务丢弃其工作进程组，验证下一个任务在新进程组中执行且结果不含前一任务的残留条目

## 6. 写入语义：upsert 与结果集归属

- [x] 6.1 把 `src/qsearch/webui/result_store.py` 的 DELETE-CASCADE 改为 `queries` 的 upsert（更新 `top_n`/config/`processing_time_ms`），验证对同一 Query 图重搜后标注库中该 Query 仍只有一行
- [x] 6.2 实现 `candidates` 的 upsert，`SET` 列表只含 rank、match_type、confidence_level、各项 score 与 hash_distance，验证一个已标注为命中的候选在重搜后评分被更新而 label、notes、labeled_at 不变
- [x] 6.3 实现结果集归属维护：本次未出现的候选置 `in_current_result = 0`，重新出现的置回 1，验证候选掉出结果集后其标注保留且标记为 0、下次重新命中后标记回 1 且标注仍不变
- [x] 6.4 让新出现的候选以未标注状态插入，验证重搜引入的新 Match 图其 label 为空

## 7. 页面化

- [x] 7.1 在 `src/qsearch/webui/app.py` 以 `st.navigation` / `st.Page` 取代顶层 `st.tabs`，注册主页、详情、搜索、标注四个页面，验证在搜索页提交查询时标注页与任务监控页的渲染逻辑不被执行（以渲染计数或日志断言）
- [x] 7.2 主页只含数据集管理与任务监控两个标签页，验证首页不出现搜索、标注、详情标签且默认选中数据集管理
- [x] 7.3 实现 URL 参数承载 `db` 与 `query`，验证在标注页刷新浏览器后仍停留在同一数据集同一查询，且该 URL 在另一会话打开进入相同视图
- [x] 7.4 让视图偏好（网格列数、专注模式）只存于会话，验证调整列数后当前 URL 不含列数参数
- [x] 7.5 实现 URL 参数校验：`db` 或 `query` 指向不存在的对象时给可读提示并回主页，验证构造的非法参数不产生未捕获异常
- [x] 7.6 验证同名部件不再冲突：两个页面各含一个标签与选项相同的数据库选择器，页面切换与渲染均不报部件标识重复错误

## 8. 数据集管理页

- [x] 8.1 让数据集列表除名称列外各字段在列内居中，验证图像数量、索引状态、命中率三列的内容居中呈现
- [x] 8.2 把列表行操作收敛为详情、搜索、标注三个跳转按钮，验证行内不再出现删除与建索引按钮
- [x] 8.3 让三个跳转带入该行数据集，验证点击搜索后进入搜索页且已选中该数据集、无需重新选择
- [x] 8.4 新建数据集详情页并在其中提供删除（含明确确认与将被移除内容的说明），验证确认后删除成功、提示结果、回到列表且该数据集不再出现
- [x] 8.5 让详情页呈现配置来源、图像数量、索引状态与标注统计，并提供进入搜索与标注的入口，验证四项信息齐全且两个入口带入该数据集
- [x] 8.6 让删除失败时给出可读原因且数据集仍存在，验证构造的失败场景下数据集未被移除
- [x] 8.7 移除创建表单的「创建后加入构建队列」复选框，改为创建成功后自动入队索引构建，验证创建成功后队列中出现该数据集的索引构建任务
- [x] 8.8 创建成功后清空表单各项输入，验证名称、图像来源与配置恢复初始状态；创建失败时保留输入，验证失败后已填内容仍在
- [x] 8.9 创建成功后提示可前往任务监控查看构建进度，验证提示文案出现且指向任务监控
- [x] 8.10 让索引构建入队失败时数据集仍保留并给出可读原因与后续动作，验证构造的入队失败场景下数据集存在且提示可操作
- [x] 8.11 保留对已建索引数据集的重建入口（置于详情页），验证触发重建后入队成功且无需重新提供图像来源

## 9. 搜索页与筛选

- [x] 9.1 把单图查询改为经交互车道执行：提交后入队并以 `st.fragment` 轮询任务状态，验证提交立即返回且等待期间当前页面保持可交互
- [x] 9.2 让轮询在任务进入终态后停止并自动呈现结果或错误，验证完成后无需用户操作即呈现结果、且轮询不再继续
- [x] 9.3 验证等待不阻塞他人：一个会话等待查询完成时，另一会话可正常浏览与操作各页面 —— 需两个并发浏览器会话，待手动验证
- [x] 9.4 保留三种输入方式并在提交前校验，验证本地路径不存在时提示且不允许提交
- [x] 9.5 对未建索引的数据集阻止提交检索并提示需先构建索引及可在任务监控查看进度，验证提交被阻止且提示出现
- [x] 9.6 移除批量检索的并行数滑块，验证提交界面无并行数控件且任务仍正常入队
- [x] 9.7 空批量输入不予受理，验证无有效图像时提交被阻止并给出提示
- [x] 9.8 实现按标注进度筛选 Query 的四个条件（一个都未标注、部分标注、已标注但无命中、无候选），且只依据 `in_current_result = 1` 的候选，验证四个条件各自返回的 Query 集合与手工构造的预期一致
- [x] 9.9 让筛选无结果时给出明确空结果提示，验证不出现空白区域
- [x] 9.10 支持从筛选结果直接进入某 Query 的标注界面并保留数据集与 Query 上下文，验证进入后无需重新选择数据集

## 10. 标注页

- [x] 10.1 让标注界面默认只呈现 `in_current_result = 1` 的候选，验证含历史候选的 Query 默认视图中不出现历史候选
- [x] 10.2 提供查看历史候选的开关，验证开启后历史候选一并呈现且可辨识其历史身份
- [x] 10.3 让标注进度只依据当前结果集计算，验证当前结果集内候选全部标注时 query 显示为已完成（即使存在未标注的历史候选）

## 11. 图标体系

- [x] 11.1 自托管 Material Symbols 字体资源并接入样式，验证在断网环境下页面图标仍正常呈现
      —— 已由框架自带满足，无需自建：Streamlit 静态包内已含
      `static/media/MaterialSymbols-Rounded.Bc71WqoX.woff2`，并在
      `static/css/index.*.css` 中以相对 `url(../media/...)` 声明 `@font-face`
      （family `Material Symbols Rounded`、`font-display: block`）；
      `index.html` 无任何 `fonts.googleapis.com` / `fonts.gstatic.com` 引用，
      故断网下图标由本地字体渲染。注意字型为 **Rounded**（非 Outlined）。
- [x] 11.2 把部件类图标改为 `icon=":material/xxx:"`（按钮、页面、标签页、提示框），验证各页面部件图标正常呈现且不再出现 emoji
      —— `st.button` 全部改用 `icon=` 形参；`st.Page`／`set_page_config` 用
      `icon=`；标签页与标题类无 `icon=` 形参，按 Streamlit 支持的
      Markdown 短代码内联。全部 34 个图标名已逐一比对
      `streamlit.material_icon_names.ALL_MATERIAL_ICONS` 确认存在
      （非法名会在渲染时抛 `StreamlitAPIException`）。
- [x] 11.3 把 `components.py` 的 chip / badge 改为 CSS class 加字体 `::before` 注入，验证 chip 与 badge 图标正常呈现
      —— chip/badge 走 `unsafe_allow_html`，拿不到 Markdown 短代码，故在
      `chips.css` 新增 `.qs-icon` 层，`content` 写连字名，复用 Streamlit
      自托管的 `Material Symbols Rounded`。
- [x] 11.4 清除各页面文案与状态标记中充当图标的 emoji，验证 `app.py`、`ui/*.py` 中不再含 emoji 图标字符
      —— 已全量扫描 `src/qsearch/webui/**/*.py`：充当图标的 emoji 与几何
      字形（✓ ✗ ○ ▲ ⊘ ◷ ◔ – ◌）已清零。保留项均非图标：正文里的破折号
      `——`、表格空值占位 `—`、以及 `styles_audit.py` 生成对比度报告用的
      ✓/✗ 标记（终端 Markdown 报告，非页面 UI）。
- [x] 11.5 让每个仅含图标的操作具备可访问的文本名称，验证辅助技术读取纯图标按钮时能得到描述其动作的名称
      —— 本次未留纯图标按钮：标注三键原为 `✓ 1`／`✗ 0`／`– S`（字形+键位，
      无动作名），现为 `icon=` + 文本标签「命中／未命中／跳过」+ `help=`
      完整动作说明。
- [x] 11.6 保持同一状态族内图标字形互相区分，验证标注结果四取值使用四个形状不同的图标、任务状态五取值使用五个形状不同的图标
      —— 标注四取值：`check_circle`（对勾圆）／`cancel`（叉圆）／
      `do_not_disturb_on`（横杠圆）／`radio_button_unchecked`（空心圆），
      四个形状互不相同。任务五取值：`hourglass_empty`／`sync`／`task_alt`／
      `error`／`block`，五个形状互不相同（另有 unknown 用 `pending`）。
- [x] 11.7 验证图标缺失时语义不丢失：模拟字体不可用，确认用户仍能通过文本判断元素含义
      —— 结构上保证而非依赖实测：每个 chip/badge/按钮都自带完整中文文案
      （「命中」「索引未建」「调度器未运行」等），图标是并列的冗余通道。
      字体不可用时 `::before` 的连字会露出连字名（难看但不改变语义），
      文案本身仍完整表意。
- [x] 11.8 保持人的判断与机器输出的视觉分离，验证一个同时带高置信与命中标注的候选，两者在形态与饱和度上可清楚区分
      —— 沿用既有 D2 纪律并在图标层保持：置信 chip 无图标、低饱和描边
      （`confidence_chip` 不加 `.qs-icon`）；命中 badge 带图标、高饱和实心。
      形态（描边/实心、无图标/有图标）与饱和度两条通道均不重叠。

## 12. 任务监控页

- [x] 12.1 让任务监控呈现车道信息与实际并行度（不再读已删除的任务级并行数列），验证每个任务显示的并行度为其车道实际使用的值
- [x] 12.2 验证任务监控不被交互查询淹没：连续提交若干单图查询并等待清理窗口过后，确认过期交互行已消失而后台历史仍在
- [x] 12.3 验证调度进程未运行时的呈现：停掉调度进程，确认任务监控明确指出这一点并说明任务会排队但暂不执行

## 13. 启动脚本

- [x] 13.1 在 `scripts/start_webui.py` 新增 `--interactive-workers`，验证 `--help` 列出该参数及其默认值与上限
- [x] 13.2 让两个并行度参数分别生效，验证指定后台 8、交互 2 时调度进程日志显示两车道各自的池规模
- [x] 13.3 让越界值收敛并输出实际生效值，验证后台超过 CPU 核数时收敛为核数并告警、交互超过上限时收敛为上限并告警
- [x] 13.4 让小于 1 的并行度拒绝启动并给出可读错误，验证退出码非零且错误信息指明原因
- [x] 13.5 未指定时取默认值，验证后台按机器资源推算、交互取默认 2，且启动输出打印两者实际值

## 14. 集成验证

- [x] 14.1 验证车道隔离的端到端行为：提交一个大规模索引构建，在其运行期间提交单图查询，确认查询在索引构建结束前完成
- [x] 14.2 验证模型复用的端到端行为：连续两次单图查询，确认第二次的响应时间显著低于第一次（不含模型加载开销）
- [x] 14.3 验证跨数据集复用：交替查询两个同配置数据集各两次，确认工作进程未被销毁重建且模型未重新加载
- [x] 14.4 验证重搜保全标注：对一张 Query 图检索、标注若干候选、换配置重搜，确认原有标注全部保留、掉出结果集的候选被标记、新候选为未标注
- [x] 14.5 验证界面重启不中断任务：索引构建运行期间关闭并重开网页，确认任务继续且新界面能看到进度
- [x] 14.6 验证中断任务可回收：强杀调度进程后重启，确认残留的运行中任务被回收为排队中并告知回收数量
- [x] 14.7 走通完整闭环：创建数据集 → 自动建索引 → 单图查询 → 筛选未标注 Query → 标注 → 查看统计，确认全流程无未捕获异常

## 15. 设计系统验证（并入 redesign-webui-dark-workbench 10.1-10.6）

- [x] 15.1 计算全部文本/背景令牌组合的对比度，验证正文 ≥4.5:1、大号标题 ≥3:1，产出对照表
- [x] 15.2 以灰度渲染各页面截图，验证标注状态、置信等级、索引状态、任务状态在无色彩信息时仍可区分
- [x] 15.3 核查全部过渡动效时长，验证状态反馈 ≤100ms、浮层进出 ≤150ms
- [x] 15.4 仅用键盘走通完整标注闭环（选库 → 搜索 → 网格标注 → 跳转未标注 → 查看统计），验证无需鼠标即可完成且焦点始终可见
- [x] 15.5 在钉定框架版本的相邻补丁版本上启动一次，验证自定义样式未丢失或错位
- [x] 15.6 记录标注类操作的实际交互延迟，验证产出实测数据作为后续优化的基线
