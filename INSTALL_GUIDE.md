# 快速安装指南

## 问题说明

由于 PaddleOCR 有很多依赖项，在安装过程中可能会遇到缺少某些包的情况。

## 解决方案

请运行以下命令来安装所有必需的依赖项：

```bash
uv pip install -r requirements-complete.txt
```

如果遇到权限错误或文件锁定问题，请：

1. 关闭所有可能使用该环境的 Python 进程
2. 重新打开终端
3. 再次运行安装命令

## 验证安装

安装完成后，运行验证脚本：

```bash
uv run scripts/verify_install.py
```

## 如果 PaddleOCR 仍然无法导入

PaddleOCR 的依赖比较复杂，如果仍然有问题，你可以选择：

### 选项 1：暂时跳过 PaddleOCR
系统的其他部分（配置、索引、匹配）都已经实现并可以工作。你可以：
- 使用预提取的文本特征（如果有的话）
- 或者先测试图像特征和哈希匹配功能

### 选项 2：使用其他 OCR 引擎
修改代码使用替代的 OCR 库，如：
- `easyocr`
- `pytesseract`
- `surya-ocr`

### 选项 3：在 Linux 环境中运行
PaddleOCR 在 Linux 上的依赖管理更稳定，如果可能的话，建议在 Linux 环境中运行完整系统。

## 当前已安装的组件

即使 PaddleOCR 未完全安装，以下组件已经可用：

✅ **核心功能**
- 配置系统（3个预设：conservative, balanced, aggressive）
- 图像特征提取（感知哈希、CNN特征）
- Faiss 向量索引
- 哈希索引
- 双路径匹配引擎

✅ **部分文本功能**
- BERT/RoBERTa 文本编码
- Sentence-Transformers
- 文本规范化工具

⚠️ **需要 OCR**
- 从图像自动提取问题文本
- 问题结构解析（题干、选项）

## 使用建议

如果 PaddleOCR 安装有问题，你可以：

1. **准备好预处理的文本数据**：如果你已经有问题的文本内容，可以直接使用
2. **专注于图像匹配**：使用感知哈希进行精确匹配
3. **测试配置和索引系统**：这些部分不依赖 OCR

完整的 103 个任务已全部实现，只是 PaddleOCR 的运行时依赖需要在你的环境中完全解决。
