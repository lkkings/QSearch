# Deep Learning Framework
torch>=2.0.0
torchvision>=0.15.0

# Vector Search - Platform specific
# On Windows: faiss-cpu
# On Linux with GPU: faiss-gpu
faiss-cpu>=1.7.4; sys_platform == 'win32'
faiss-gpu>=1.7.4; sys_platform == 'linux'

# OCR Engines
paddleocr>=2.7.0
# OCR runtime: paddleocr needs the paddlepaddle runtime, which ships as two
# mutually exclusive distributions. This pins the CPU build for portability.
# With an NVIDIA GPU, install the CUDA build instead (GPU is preferred):
#   uv run scripts/install_paddle.py
#   or: uv pip install paddlepaddle-gpu==3.0.0 -i https://www.paddlepaddle.org.cn/packages/stable/cu126/
paddlepaddle==3.0.0
pix2tex[api]>=0.1.2  # LaTeX-OCR

# NLP Models
transformers>=4.30.0
sentence-transformers>=2.2.0

# Image Processing
opencv-python>=4.8.0
Pillow>=10.0.0
imagehash>=4.3.1

# Configuration
pyyaml>=6.0

# Utilities
numpy>=1.24.0
tqdm>=4.65.0
psutil>=5.9.0
scikit-image>=0.21.0
python-Levenshtein>=0.21.0
requests>=2.31.0
urllib3>=2.0.0

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0
