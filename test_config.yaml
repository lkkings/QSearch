# Test configuration file for ConfigLoader verification
text:
  components:
    stem:
      enabled: true
      weight: 0.6
    options:
      enabled: true
      weight: 0.3

matching:
  exact_match:
    threshold: 5
  content_match:
    threshold: 0.85
